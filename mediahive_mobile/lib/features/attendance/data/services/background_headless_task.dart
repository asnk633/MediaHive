/// Headless task handler — STUB.
///
/// The original used `flutter_background_geolocation` headless tasks.
/// This stub is a no-op placeholder until Phase 2 replaces it with
/// `workmanager` background callbacks.
///
/// Registered in main.dart — must remain a top-level function.
@pragma('vm:entry-point')
void backgroundGeolocationHeadlessTask(dynamic headlessEvent) async {
  // No-op: flutter_background_geolocation removed (paid license required).
  // Phase 2 will replace this with workmanager callbacks.
}
