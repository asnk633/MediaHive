"use client";

import { ReactNode } from "react";

console.log('[STATIC CHECK] app/(shell)/flowboard/layout.tsx');

export default function flowboardLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
