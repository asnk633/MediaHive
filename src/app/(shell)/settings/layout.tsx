"use client";

import { ReactNode } from "react";

console.log('[STATIC CHECK] app/(shell)/settings/layout.tsx');

export default function settingsLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
