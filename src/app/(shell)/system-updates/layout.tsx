"use client";

import { ReactNode } from "react";

console.log('[STATIC CHECK] app/(shell)/system-updates/layout.tsx');

export default function SystemUpdatesRootLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
