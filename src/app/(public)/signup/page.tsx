import SignupClient from "@/components/auth/SignupClient";

export const metadata = {
    title: "Create Account | MediaHive",
    description: "Join the MediaHive production team.",
};

export default function SignupPage() {
    return <SignupClient />;
}
