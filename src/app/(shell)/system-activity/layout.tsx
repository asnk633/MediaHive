"use client";

import { ReactNode } from "react";

console.log('[STATIC CHECK] app/(shell)/system-activity/layout.tsx');

export default function SystemActivityLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
