'use client';

import { ReactNode } from "react";

console.log('[STATIC CHECK] app/(shell)/updates/layout.tsx');


export default function UpdatesRootLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
