"use client";

import { ReactNode } from "react";

console.log('[STATIC CHECK] app/(shell)/media/layout.tsx');

export default function mediaLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
