"use client";

import { ReactNode } from "react";

console.log('[STATIC CHECK] app/(shell)/calendar/layout.tsx');

export default function CalendarRootLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
