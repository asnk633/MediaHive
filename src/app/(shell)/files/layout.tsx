"use client";

import { ReactNode } from "react";

console.log('[STATIC CHECK] app/(shell)/files/layout.tsx');

export default function filesLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
