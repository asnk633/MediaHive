"use client";

import { ReactNode } from "react";

console.log('[STATIC CHECK] app/(shell)/reports/layout.tsx');

export default function ReportsRootLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
