"use client";

import { ReactNode } from "react";

console.log('[STATIC CHECK] app/(shell)/campaigns/layout.tsx');

export default function campaignsLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
