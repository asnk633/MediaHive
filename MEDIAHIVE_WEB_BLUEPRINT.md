# MediaHive Web App & Core API Blueprint

This document is the platform blueprint and system memory for the **MediaHive Web App & Core API** (Next.js context).

---

## 1. Core Architecture Overview
* **Framework**: Next.js App Router (`^16.0.7`) + React (`^19.2.1`)
* **State Management**: Jotai for local state, TanStack React Query (`^5.0.0`) for server state.
* **Authentication**: Supabase Auth (Service Account in REST APIs, user sessions in client).
* **Database / ORM**: Drizzle ORM + Supabase PostgreSQL (local sqlite fallback via `dev.db`).

---

## 2. Platform-Specific Quirks & Constraints

### 🔌 HTTP/1.1 Socket Starvation (SSE Connections)
* **Quirk**: In local development environments, Next.js serves assets over HTTP/1.1. Browsers restrict active sockets to the same domain to exactly 6.
* **Impact**: Opening multiple persistent Server-Sent Events (SSE) connections (such as subscribing to notifications from multiple components) blocks all other API requests, causing extreme slowness and loading delays.
* **Constraint**: Any SSE client (e.g., `listenNotifications`) must run through a unified Pub/Sub manager to share a single connection per user, keeping active sockets to exactly 1.

### ⏱️ Sequential API Loops (N+1 Query Bottleneck)
* **Quirk**: Performing database queries sequentially inside a loop (e.g., fetching room messages and unreads) blocks execution threads and accumulates network roundtrip latency.
* **Constraint**: Loop operations fetching room/task metadata must be parallelized concurrently using `Promise.all` to limit sequential database roundtrips.

---

## 3. Web Changelog

| Date | Component | Description | Author |
| :--- | :--- | :--- | :--- |
| 2026-06-30 | Titlebar & Platform | **Tauri Titlebar Colors & Synchronous isTauri Detection:** Standardized the window titlebar text and window control button hover styles in `Titlebar.tsx` to use standard tailwind colors (`text-zinc-400` and `hover:text-white`). Refactored `useIsTauri.ts` and `WindowContext.tsx` to initialize the `isDesktop` state synchronously by instantly checking the global `window` object properties for Tauri variables (`__TAURI_INTERNALS__`, `__TAURI__`, or `isTauri`) during mount. | AI Agent |
| 2026-06-30 | Web & Desktop / Auth | **Tauri deep link callback page and AuthGuard routing:** Created `/auth/callback` page in `src/app/auth/callback/page.tsx` to handle authentication deep links back to Tauri (`mediahive://login`). Updated `loginWithGoogle` in `AuthContextProvider.tsx` to redirect to the new callback URL (`/auth/callback?source=tauri`). Added `/auth/error` and `/auth/callback` to `NO_SHELL_ROUTES` in `ShellWrapper.tsx`. | AI Agent |
| 2026-06-30 | Web & Desktop / UI Redesign | **Restored Welcome Page Cocktail Styling:** Reapplied the Honey Yellow re-theme (#d99b16), ArrowLeft icon components, full-viewport shooting star background alignment, larger 160px centerpiece workspace logo, and clean single-line profile tooltips inside `src/app/login/page.tsx`, preserving form input defaults. Verified with clean Turbopack production compilation. | AI Agent |
| 2026-06-29 | Desktop / UI Glassmorphism | **Professional Glassmorphism & Depth Upgrade:** Transitioned solid panel backgrounds to translucent values (`rgba`) inside dark mode definitions. Added high-fidelity GPU-accelerated backdrop blur (`backdrop-filter`) to sidebar, top header, dialog modals, right details drawer, metric pods, and studio cards. Fixed Tailwind color opacity rendering bugs by replacing raw variables with direct translucent assets. Added organic, slow-drifting Honey Yellow radial ambient background glows (`::before` / `::after`) behind the layout panels. Added smooth 3D translate-Y transforms and elevation drop-shadows on card hover states. | AI Agent |
| 2026-06-29 | Desktop / Typography | **Mintlify Typography Alignment:** Imported `Geist_Mono` from `next/font/google` alongside `Inter` in `layout.tsx`. Resolved a circular CSS variable reference bug in `globals.css` that was shadowing the Next.js google font loader variable and defaulting to system fallback fonts. Configured custom font family variables `--font-sans` and `--font-mono` mapped to Tailwind v4. Added typography overrides for all headings `h1` through `h6` with matching Weights (600), Line-Heights (1.15 - 1.4), and Negative Letter-Spacing (-0.02em to -0.01em) matching Mintlify's design scale, and bound all code elements to Geist Mono. | AI Agent |
| 2026-06-29 | Desktop / UI Polish | **Mintlify-Honey Theme Polish:** Cleaned up and re-themed remaining green/teal/indigo accent elements across all operational pages (Chat, Inventory, Settings, Governance, Insights, Auth Error, Notifications, Events, Downloads, Profile, Leave Management). Re-themed `BreadcrumbTabs.tsx` active sub-navigation pills, settings tabs, and inventory pills to use a new `.active-tab-capsule` utility class defined in `globals.css` for reliable background and border rendering. Re-themed chat sidebar rooms, avatars, unread counts, text fields, and sandbox loaders. Run `pnpm build` to verify 100% build health. | AI Agent |
| 2026-06-29 | Desktop / UI Redesign | **Mintlify-Honey Redesign (Phases 1-4 Complete):** Completed full desktop client transition to Honey Yellow `#d99b16` and Two-Tone Charcoal theme. Redesigned left collapsible sidebar (search pill, uppercase sections, no left active border), dashboard header and metrics cards, task list rows (bottom separators, selected left border indicator), and split-screen login forms (flat cards, honey pills). Applied selective glassmorphism (backdrop-blur) on headers, right detail drawer, and dialog modals. Verified clean production build compilation. | AI Agent |
| 2026-06-29 | Desktop / UI Redesign | **Mintlify-Honey Redesign (Phase 1 & 2):** Transitioned desktop client styling to Honey Yellow `#d99b16`, dark `#0a0a0a` / `#111111` / `#171717` panels, hairline 1px borders, active button scaling, and custom outline focus-ring. Updated `DesktopShell.tsx` collapsible sidebar with search trigger pill (`Search... ⌘K`), uppercase section labels, and styled active items with no left border line and an active bg wash. | AI Agent |
| 2026-06-29 | Desktop / UI Redesign | **Mintlify Green UI transition:** Transitioned styling to Mintlify green HEX tokens (`#00d4a4` / `#00b48a`) and hairline 1px borders. Deactivated `.panel-accent-top`. Cleaned up greetings, subtitles, and metrics row on Dashboard and Tasks pages. Shrunk MetricPod paddings and reduced digit size to text-[22px]. | AI Agent |
| 2026-06-29 | Desktop / UI Redesign | **CSS class names cleanup pass:** Cleaned up and renamed all legacy class selectors (`glass-card-premium`, `glass-card`, `glass-panel`) to the new flat layout equivalents (`studio-card-premium`, `studio-card`, `studio-panel`) across all pages, layouts, and components in the Next.js desktop client codebase. | AI Agent |
| 2026-06-29 | Desktop / UI Redesign | **Complete Studio-Command UI visual redesign:** Removed old "AI-designed" glassmorphism, glowing canvas orbits, cursor coordinates lighting, and star particles. Implemented flat layout color system (adaptive dark true-black `#000000` / `#1c1c1e` and light white `#ffffff` / `#f5f5f7` tokens), collapsible left sidebar (`56px` to `240px`), top header (`48px`), and collapsible Right Detail Drawer (`280px`). Redesigned all dashboard components (`MetricPod`, `TaskRow`, `ScheduleItem`, `RequestsWidget`), error/not-found layouts, `AuthGuard` loading screen, and a premium split-screen login/signup interface. Verified clean compilation on `pnpm build`. | AI Agent |
| 2026-06-28 | Tasks / Kanban Board | **Kanban DnD rewrite — replaced broken native HTML5 API with `@dnd-kit/core` v6.3.1.** Completely replaced the unreliable native HTML5 Drag and Drop implementation (which silently failed in WebView2/Tauri due to `dataTransfer.getData()` returning empty and race conditions between `handleDragEnd` and `handleDrop`) with `@dnd-kit/core`. Created modular components `KanbanCardView`, `KanbanCardDraggable` (using `useDraggable`), and `KanbanColumnDroppable` (using `useDroppable`), wrapping the board in `<DndContext>` + `<DragOverlay>`. | AI Agent |
| 2026-06-28 | Desktop Pages | Wired Tasks and Chats view synchronization with URL query parameters to support Board View and Channels, moved Leave Management page to `/leave` route, and fixed Calendar events database schema columns (start_at, end_at, created_by) and local timezone-safe helper parsing. | AI Agent |
| 2026-06-28 | Desktop / Navigation | Wired up the top bar navigation breadcrumbs/pills in DesktopShell.tsx to dynamically route via Next.js Link components. Extracted routing logic into a separate Suspense-wrapped BreadcrumbTabs.tsx component using useSearchParams() to prevent layout deoptimization and solve React server-client hydration mismatches. Added `.no-scrollbar` styling and `min-w-0` to the flex container to prevent layout scrolling issues. | AI Agent |
| 2026-06-28 | Desktop / Modals | Changed dialog modal content wrappers to a flex-column layout with `!flex !flex-col !gap-4 max-h-[90vh] overflow-y-auto`, updated the App Router height propagation selectors in `globals.css` using `:not(:has([data-slot="dialog-portal"])):not(:has([data-slot="dialog-content"]))`, and added `!h-auto` to `DialogContent` to explicitly override Next.js App Router body selectors. | AI Agent |
| 2026-06-28 | Desktop / Modals | Fixed layout display and stretching bugs in Quick Create modals by adding `!fixed` (equivalent to `position: fixed !important`) to `DialogContent` in `dialog.tsx` to override `.glass-panel`'s `position: relative`, and by adding `:not([data-radix-portal])` to the Next.js App Router height propagation rules in `globals.css` to exclude Radix portal elements. | AI Agent |
| 2026-06-28 | DB Schema Check | Fixed dotenv loading order and key fallback in checkSchema.ts to verify Supabase alignment correctly. | AI Agent |
| 2026-06-28 | Build Config | Migrated deprecated Sentry properties (disableLogger, automaticVercelMonitors) to webpack-nested structures. | AI Agent |
| 2026-06-28 | Health Checks | Resolved DTO Mapping schema checks, removed dead links, and deleted empty API directory. | AI Agent |
| 2026-06-28 | Tasks / Permissions | Allowed 'manager' role to view TaskConfidenceView and AdminConfidencePanel. | AI Agent |
| 2026-06-28 | SSE / Notifications | Rewrote `notificationRealtime.ts` to implement connection sharing Pub/Sub. | AI Agent |
| 2026-06-28 | Chat API / Performance | Parallelized room details queries in `/api/chat/rooms` GET using `Promise.all`. | AI Agent |
| 2026-06-28 | API Typecheck | Resolved TypeScript errors across API endpoints and db seed script, enabling clean Next.js production builds. | AI Agent |
| 2026-06-28 | Auth / Desktop | Fixed session check race condition and premature redirect by immediately setting fallback user state from session metadata, preventing the 10-second safety timeout from triggering a login redirect while database profile queries resolve. | AI Agent |
| 2026-06-29 | Web / UI Redesign Setup | **Phase 1: Foundation & Theme Setup.** Created client-side `ThemeContext.tsx` with light/dark theme support and persistent `localStorage` sync. Rewrote `globals.css` to establish flat design system variables, standard font properties (Inter/system-monospace), and legacy theme variable bridges while removing old glassmorphism, glowing styling, noise, and drift animations. Modified `layout.tsx` to wrap children in the new `ThemeProvider` and replaced DM Sans and Outfit fonts with Inter. Verified production build compiles successfully with zero compile or typescript errors. | AI Agent |
| 2026-06-30 | Web & Desktop / UI Redesign | **Welcome Page Redesign.** Overhauled `src/app/login/page.tsx` with a high-fidelity atmospheric universe theme, featuring GPU-accelerated CSS auroras (centered and repositioned behind the login card for high WebView2 contrast), command-line boot visualizer simulation with blink cursor and online ping indicator, glassmorphic auth cards with Framer Motion transitions (layout-aware form switches), input element double-border active states, glass-like input background adjustments, rich gold gradient session buttons, and shake animation errors. Compiles and type checks successfully on Next.js 16.2.7. | AI Agent |
| 2026-06-30 | Web & Desktop / UI Redesign | **Restored Previous Concentric Orbit Welcome UI.** Overhauled `src/app/login/page.tsx` back to the preferred design with concentric dotted line orbits carrying animated letters/avatars, a central white maze logo, and nebulous green/teal auroral clouds. Restored missing dynamic visualizer dependencies (`shooting-stars.tsx`, `stars-background.tsx`, `etheral-shadow.tsx`) and the custom `HaloLogo` components. Compiles and builds successfully. | AI Agent |
| 2026-06-30 | Web & Desktop / UI Redesign | **Re-themed Orbits Welcome UI to Honey Yellow.** Modified `src/app/login/page.tsx` to align the orbits interface with the flat/charcoal/hairline styling. Re-themed tasks and background nodes from Teal to Honey Yellow (`#d99b16`, rgb `217,155,22`), mapped all teal gradient buttons to Honey Yellow-to-Amber gradients, and converted top-right collapsed decorative box card gradients to warm gradients (amber, brown, orange). Verified build compiles successfully. | AI Agent |
| 2026-06-30 | Web & Desktop / UI Redesign | **Fix back button Unicode arrow glyphs.** Replaced corrupted Unicode characters (`ΓåÉ` / `ΓÅÉ`) inside all back buttons with the Lucide `ArrowLeft` component to prevent mojibake encoding mismatch issues in Tauri webviews. Verified compilation. | AI Agent |
| 2026-06-30 | Web & Desktop / UI Redesign | **Cleaned Silky Background Noise Overlay.** Modified `src/components/ui/etheral-shadow.tsx` to remove high-CPU SVG turbulence displacement filters and obsolete maskImage URL references. Replaced with highly-optimized, GPU-accelerated CSS drifting ambient radial gradients using hardware-composited transitions, reducing Tauri container CPU utilization. Verified project build is clean. | AI Agent |
| 2026-06-30 | Web & Desktop / UI Redesign | **Relocated UnifiedStars to Top-Level Parent Frame.** Moved the `<UnifiedStars />` shooting star canvas container in `src/app/login/page.tsx` from the login form container on the right side to the top-level parent container wrapper. This renders the particle system across the entire screen (behind both orbits and forms) and prevents duplicate instantiation. Verified project build is clean. | AI Agent |
| 2026-06-30 | Web & Desktop / UI Redesign | **Removed Stat Subtext from Coin Tooltips.** Edited the `Icon` component in `src/app/login/page.tsx` to remove the conditional `iconData.stat` subtext line, keeping only the main label title for a cleaner, less cluttered user profile presentation on orbit hovers. Verified build compiles successfully. | AI Agent |
| 2026-06-30 | Web & Desktop / UI Redesign | **Scaled Central Workspace Logo.** Increased the centerpiece logo (`/media-app-logo-golden.png`) dimensions from `110 * scaleFactor` to `160 * scaleFactor` to make it visually stand out as the hero element over standard rotating orbit coins. Verified build compiles successfully. | AI Agent |
| 2026-06-30 | Database / Cleanup | **Purged Test & Guest Accounts from Supabase.** Executed SQL scripts to permanently delete 107 obsolete `guest_%@test.com` accounts and 79 test `testuser_%@example.com` accounts from the `auth.users` schema. Cascade constraints automatically cleaned the `public.profiles` table, leaving exactly 8 active real profile records. | AI Agent |
| 2026-06-30 | Web & Desktop / UI Redesign | **Customized Input Placeholders & Refactored Button Labels.** Updated auth form fields in `src/app/login/page.tsx` replacing generic placeholder strings: set Full Name to 'Shukoor Rahman', set Email inputs to 'you@thaibagarden.com', and blanked out default password bullet masks. Re-labeled 'Register Node' and 'profile node' elements to 'Register User' and 'user profile' for cleaner accessibility terminology. Verified build. | AI Agent |






