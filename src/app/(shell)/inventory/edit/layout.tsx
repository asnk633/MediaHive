"use client";

import { ReactNode } from "react";

console.log('[STATIC CHECK] app/(shell)/inventory/edit/layout.tsx');

export default function inventoryLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
