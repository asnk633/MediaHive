"use client";

import { ReactNode } from "react";

console.log('[STATIC CHECK] app/(shell)/activity/layout.tsx');

export default function ActivityRootLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
