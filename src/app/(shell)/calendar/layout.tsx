'use client';
export const dynamic = 'force-dynamic';


import { ReactNode } from "react";

export default function CalendarRootLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
