"use client";

import { ReactNode } from "react";

console.log('[STATIC CHECK] app/(shell)/profile/layout.tsx');

export default function profileLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
