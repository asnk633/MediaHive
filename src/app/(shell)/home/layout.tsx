"use client";

import { ReactNode } from "react";

console.log('[STATIC CHECK] app/(shell)/home/layout.tsx');

export default function homeLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
